#include<stdio.h>
int main ()
{
int i,n,S = 0;
printf(" Enter value of n = " );
scanf(" % d" , &n);
for (i = 1; i <=n ; i++)
{
S = S + i * i;
}
printf(" final value of S = % d" , S);
return 0;
printf ("suryakant is a dumb guy");
}